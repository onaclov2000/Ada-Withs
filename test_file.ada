with This; -- This is a test comment.
with That;

    Call.ThePackage;


-- Sometimes you see .'s in a say if then statement. so we really need to parse those kinds of keywords out of the string...per se

if This.Is(The_Call) or else That.Is(The_Call) then
   Give_it_a_try := 1;
   Or.Else(Give_This_Is_A_Call_Too);
end if;
